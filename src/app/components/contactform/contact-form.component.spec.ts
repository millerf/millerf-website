import { ContactFormDirective } from './contact-form.component';

describe('ContactFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactFormDirective();
    expect(directive).toBeTruthy();
  });
});
